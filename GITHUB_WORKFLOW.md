# GitHub Workflow 가이드

## 🎯 Git Flow 브랜치 전략

### 브랜치 구조

```
main (제품 출시 브랜치)
  └── develop (개발 브랜치)
       ├── feature/login-page
       ├── feature/home-page
       ├── feature/movie-api
       └── ...
```

### 브랜치 설명

- **main**: 항상 배포 가능한 안정적인 상태 유지
- **develop**: 모든 기능 개발이 통합되는 개발 브랜치
- **feature/***: 각 기능별 개발 브랜치

## 📋 작업 흐름

### 1. 새 기능 개발 시작

```bash
# develop 브랜치로 전환
git checkout develop

# 최신 코드 가져오기
git pull origin develop

# 새 기능 브랜치 생성
git checkout -b feature/기능명

# 예시:
git checkout -b feature/user-authentication
git checkout -b feature/home-page
git checkout -b feature/movie-card
```

### 2. 개발 및 커밋

```bash
# 파일 수정 후
git add .
git commit -m "feat(scope): 명확한 커밋 메시지"

# 여러 번 커밋 가능
git add src/components/MovieCard.js
git commit -m "feat(movie): MovieCard 컴포넌트 구현"

git add src/pages/Home.js
git commit -m "feat(pages): 홈 페이지에 영화 리스트 추가"
```

### 3. 원격 저장소에 푸시

```bash
# 첫 푸시 시
git push -u origin feature/기능명

# 이후 푸시
git push
```

### 4. Pull Request 생성

1. GitHub 저장소 페이지로 이동
2. "Compare & pull request" 버튼 클릭
3. PR 제목 및 설명 작성
4. `develop` 브랜치로 머지 요청
5. 리뷰어 지정 (혼자 작업 시 생략 가능)
6. "Create pull request" 클릭

### 5. PR 머지 후 브랜치 삭제

```bash
# develop 브랜치로 전환
git checkout develop

# 최신 코드 가져오기
git pull origin develop

# 로컬 feature 브랜치 삭제
git branch -d feature/기능명

# 원격 feature 브랜치 삭제 (선택사항)
git push origin --delete feature/기능명
```

## 🔀 develop → main 머지

### 충분한 기능이 쌓였을 때

```bash
# main 브랜치로 전환
git checkout main

# develop 브랜치 머지
git merge develop

# 원격 저장소에 푸시
git push origin main

# 태그 추가 (선택사항)
git tag -a v1.0.0 -m "초기 프로젝트 구조 완성"
git push origin v1.0.0
```

## 🏷️ 태깅 전략

### 주요 버전에 태그 추가

```bash
# 주요 기능 완성
git tag -a v1.0.0 -m "초기 프로젝트 구조 및 기본 페이지 완성"
git push origin v1.0.0

# 버그 수정
git tag -a v1.0.1 -m "API 키 인증 버그 수정"
git push origin v1.0.1

# 새 기능 추가
git tag -a v1.1.0 -m "위시리스트 기능 추가"
git push origin v1.1.0
```

### 태그 확인

```bash
# 모든 태그 보기
git tag

# 특정 태그 상세 정보
git show v1.0.0
```

## 📝 커밋 메시지 예시

### 좋은 커밋 메시지

```
feat(auth): 로그인/회원가입 페이지 구현

- 이메일/비밀번호 입력 폼 추가
- 유효성 검사 로직 구현
- Local Storage에 사용자 정보 저장
- 로그인-회원가입 전환 애니메이션 추가
```

```
fix(api): TMDB API 요청 시 에러 핸들링 개선

API 호출 실패 시 사용자에게 명확한 에러 메시지 표시
```

```
docs: 개발 가이드 문서 업데이트

- TMDB API 키 발급 방법 추가
- 로그인 페이지 구현 가이드 보완
```

### 나쁜 커밋 메시지

```
수정
```
```
커밋
```
```
asdf
```

## 🚀 현재 상황 정리

### 완료된 작업

1. ✅ `develop` 브랜치 생성 및 푸시 완료
2. ✅ 프로젝트 초기 구조 커밋 완료 (8개의 의미있는 커밋)
3. ✅ Git 컨벤션 문서 작성 완료
4. ✅ Pull Request 템플릿 추가 완료

### 다음 단계

#### 옵션 1: 바로 develop → main PR 생성 (권장)

현재 `develop` 브랜치에 모든 초기 구조가 완성되었으므로, 
이를 `main`에 머지하는 PR을 생성할 수 있습니다.

1. GitHub에서 PR 생성:
   - Base: `main`
   - Compare: `develop`
   - 제목: `[Chore] 프로젝트 초기 구조 설정 완료`

2. PR 머지 후:
   - `main` 브랜치에 v0.1.0 태그 추가

#### 옵션 2: 추가 개발 후 PR

추가 기능 개발 후 PR을 생성할 수도 있습니다.

## 💡 실제 개발 시나리오 예시

### 시나리오: 로그인 페이지 구현

```bash
# 1. develop에서 시작
git checkout develop
git pull origin develop

# 2. feature 브랜치 생성
git checkout -b feature/login-page

# 3. 개발 및 커밋
git add src/pages/SignIn.js
git commit -m "feat(auth): 로그인 폼 UI 구현"

git add src/services/auth.js
git commit -m "feat(auth): 로그인 로직 구현"

git add src/pages/SignIn.css
git commit -m "style(auth): 로그인 페이지 스타일 추가"

# 4. 원격 저장소에 푸시
git push -u origin feature/login-page

# 5. GitHub에서 PR 생성
# - Base: develop
# - Compare: feature/login-page
# - 제목: [Feat] 로그인/회원가입 페이지 구현

# 6. PR 머지 후
git checkout develop
git pull origin develop
git branch -d feature/login-page
```

## 🔍 유용한 Git 명령어

### 현재 상태 확인

```bash
# 현재 브랜치 확인
git branch

# 모든 브랜치 확인 (원격 포함)
git branch -a

# 변경사항 확인
git status

# 커밋 히스토리 확인
git log --oneline --graph
```

### 브랜치 관리

```bash
# 브랜치 생성 및 전환
git checkout -b feature/기능명

# 브랜치 전환
git checkout 브랜치명

# 브랜치 삭제
git branch -d 브랜치명

# 원격 브랜치 삭제
git push origin --delete 브랜치명
```

### 커밋 관리

```bash
# 마지막 커밋 메시지 수정 (아직 푸시 안 했을 때)
git commit --amend -m "새로운 커밋 메시지"

# 여러 커밋을 하나로 합치기 (interactive rebase)
git rebase -i HEAD~3

# 커밋 취소 (작업 내용은 유지)
git reset --soft HEAD~1

# 커밋 취소 (작업 내용도 삭제) - 주의!
git reset --hard HEAD~1
```

## ⚠️ 주의사항

1. **main 브랜치 직접 푸시 금지**
   - 항상 `develop` 또는 `feature` 브랜치에서 작업
   - `main`은 PR을 통해서만 업데이트

2. **커밋 전 확인**
   - 불필요한 파일 제외 (node_modules, .env 등)
   - 커밋 메시지 컨벤션 준수
   - 한 커밋에 하나의 논리적 단위만

3. **브랜치 최신화**
   - 작업 시작 전 항상 `git pull`로 최신 상태 유지
   - 충돌 발생 시 신중하게 해결

4. **테스트 후 커밋**
   - 커밋 전 빌드 및 기본 동작 확인

## 🎉 완료!

이제 Git Flow 전략을 사용하여 체계적으로 개발할 수 있습니다!

추가 질문이 있으시면 언제든지 물어보세요.

